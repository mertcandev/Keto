import 'package:flutter/material.dart';
import 'package:marquee/marquee.dart';

class KayanYazi extends StatelessWidget {
  const KayanYazi
({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Marquee(text: "#ketolivesmatter",
      
    );
  }
}